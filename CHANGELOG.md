# 变更日志

本文件记录本仓库的重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 版本号沿用产品线（`package.json` 的 `version`）。本仓库的变更自 `0.1.4` 起记录。

---

## [0.1.4-zerowork.23]

### 修复：点开 xlsx / 代码 / JSON 预览会把整个渲染层打进错误边界

**症状**：在工作区的「工作空间文件」里点开一个 `.xlsx`（或 `.js` / `.json`），
整个面板变成「界面渲染出错」，要点重新加载才能恢复。

**根因分两层，第二层是第一层的根**：

① 渲染层源码里，每个懒加载块的内联依赖表是**字面量字符串**
   （`viteMapDeps([...])`，形如 `["./office-xlsx.js", "./vendor-lodash.js", "./office-xlsx.css"]`）。
   这些字符串在**运行时**被拼成 URL 去预加载，Vite 不会改写它们 ——
   它只认 import 说明符。而重建时渲染层用的是默认的**内容哈希**命名，
   于是表里每一项都指向不存在的文件。

② 其中 CSS 那几条是**致命**的：预加载助手对 CSS 加载失败会 reject 懒加载 promise，
   未拦截的 `vite:preloadError` 直接抛出去，`<Suspense>` 兜不住，整个渲染层落进错误边界。
   （JS 那几条只是白预载，失败无副作用 —— 所以这个缺陷只在「点开某个文件」时暴露，
   冒烟与 IPC 断言全都照过。）

另外发现，这批样式表**压根没进产物**：`office-xlsx.css`（表格网格）、
`code-preview.css`（monaco）、`json-mode.css` 在还原出的 chunk 源码里没有任何 import ——
「哪个 chunk 用哪份样式」这条边在打包产物里只剩依赖表那串字面量，
于是重新构建时这三份 CSS 一个都不会被产出。就算不崩，预览也是**没样式**的。

**修法**（三处，都在「让重建产物与还原源码的假设一致」）：

| 位置 | 改动 |
| --- | --- |
| `electron.vite.config.mjs`（renderer） | 产物改用**无哈希的语义名**：入口显式命名 `app` ⇒ `app.js` / `app.css`，chunk 与资产同名配对（`[name].js` / `[name][extname]`）。依赖表里的字符串这才指得对 |
| 样式归属规则 | 给每个 chunk **补回它自己样式表的 import**（入口除外，它由 index.html 引入）。这条边在构建期被抽走，产物里不留 |
| 当前源码树 | 同一规则增量补到 `office-xlsx.js` / `code-preview.js` / `json-mode.js` 三个 chunk 上 |

**验证**：产物里 `viteMapDeps` 表引用的 **43 项全部命中**（脚本全量核对），
html 的 `app.js` / `app.css` 也在。

### 新增测试：Agent 团队（10 项）

补一处零覆盖：团队（`team_create` / `team_send` / `team_status` / `team_read` …）
是头牌能力之一，此前没有任何测试调用过。两个容易漏的前提都显式处理了：

- 团队工具**只在「Agent 团队」开启时才注册**（工厂里 `if (!deps.isEnabled()) return;`），
  默认关着，不先打开根本调不到；
- `team_create` **不等成员完成**，成员在后台跑，产出留在自己的会话记录里。

断言分两段（都落在**证据**上，不是推测）：

1. **成员真的起了独立会话** —— 读 `sessions/` 目录的 JSONL，认出带
   `customType:"team_member"` 的那份，且它的 **assistant 回复**里有成员标记；
   反向确认主代理那份里**没有**这个标记（上下文真隔离）。
2. **产出回收的两条路都通** —— 领导下一轮请求里确实带着
   `<team_output team="…"><member_output member="scout1">` 快照（断言抓的是
   mock 收到的原始请求体），`team_read` 也返回成员正文。

> 过程中修掉一个**自己埋的假通过**：最初把标记串内嵌在成员的初始任务里，
> 于是「会话文件里含标记」被成员那条**用户消息**满足 —— 成员一个字没回也照样通过。
> 改成标记不内嵌于任务文本，断言只看 assistant 消息。

### 新增测试：文件预览渲染器（7 项）

覆盖「工作空间文件」视图与**全部懒加载预览渲染器**（`office-xlsx` / `office-pptx` /
`code-preview` / `json-mode`），用真实文件：xlsx 用 `xlsx`(SheetJS) 现造，
pptx 用 `jszip` 按 OOXML 拼最小部件。

断言分三层，缺一层就抓不到对应的故障：

1. 渲染产物（`.preview-office`、canvas、工作表标签）；
2. **解析证据**：工作表标签名、幻灯片里的文本 —— 来自文件本身。
   表格是 canvas 画的，单元格文本不在 DOM 里，所以不能拿单元格内容当判据；
3. **样式表生效**：断言工作表标签的 `padding-left` 等于 `office-xlsx.css` 里的值 ——
   这条是上面那个缺陷的回归守卫（容器和 canvas 都照样在，只有这条会红）。

### 修复：测试自身污染了用户真实家目录

会话分支测试的提示词原来是「请记住这个词：…」——**记忆系统的提示词把用户级记忆的
路径写成了字面量 `~/.zerowork/MEMORY.md`**，并让模型直接 edit 那个固定路径。
模型于是绕过测试设置的 `ZEROWORK_CONFIG_DIR`，把测试残留写进了真实家目录；
下一次运行又会读到「我已经记住过了」，行为随之改变（实测：模型多调了一次记忆 `read`，
随后那一轮卡住，测试假失败）。

已清理那份残留（恢复成产品保证的空文件初始态，原文件里除测试残留外没有别的内容），
并把提示词改成**不沾记忆语义**的复述任务，附注释说明为什么不能写「记住」。
产品侧的路径分叉见 `EXTERNAL_REQUESTS.md` 第六节 6.2（未改，待你裁决）。

### 测试规模

`npm run test:gui` 现在 12 组、217 项断言，全绿；`npm test` 111 项；
慢链路（model / loop / doc / docx / skill / cmd / auto / branch）另计，全绿。

---

## [0.1.4-zerowork.22]

### 新增测试：子代理委派（6 项，用 mock 模型，确定性的）

补一处零覆盖：`task`（「把可独立完成的子任务委派给子代理，在隔离上下文中执行」）
是应用的头牌能力之一，但此前**没有任何测试调用过它**。

子代理这条链路比普通工具调用长得多：要起一个**独立会话**、带上那份 agent 人设、
跑它自己的循环，再把结果并回主代理的上下文。任何一环断了，表现都是
「模型说委派了，但什么也没发生」—— 而只断言「工具被调用了」是抓不到的。

做法：mock 按请求的**内容特征**分流，而不是靠轮次计数（子代理可能自己再调工具，
计数会错）：

- 请求里有 `role:"tool"` 且带子代理标记 ⇒ 主代理收尾轮 → 最终文本；
- 请求里以**任务描述**为用户消息 ⇒ 子代理的第一轮 → 返回子代理标记；
- 其余 ⇒ 主代理首轮 → 返回 `task` 工具调用。

断言：

1. **子代理真的被起了** —— mock 收到带任务描述的那一轮，
   且它是个**隔离会话**（实测那一轮只有 4 条消息，没有主代理之前的对话）；
2. **产出回传到主代理** —— 最终上下文里找得到子代理的标记；
3. 主代理收尾正常，最终回复渲染到界面上。

实测 mock 共收到 3 轮请求（主 → 子代理 → 主）。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| 设置页分组 | 22 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 29 |
| 对话框与降级 | 9 |
| 可视化卡片渲染 | 6 |
| 产物交付 | 6 |
| 子代理委派 | 6 |
| 主链路（mock 模型） | 11 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **370** |

## [0.1.4-zerowork.21]

### 新增测试：产物交付（6 项，用 mock 模型，确定性的）

补一处零覆盖：`present_files`（「把成果文件交付给用户」）此前只在一处**注释**里被
提到过，没有任何测试真正调用它。而它是**任务收尾的最后一环** —— 模型干完活把文件
交给用户，用户能不能在界面上看到那张卡片，全看这条链路。

做法与「可视化卡片渲染」同款：mock 模型按轮次返回不同的工具调用，走真实 UI 发送。

断言：

1. 用**合法绝对路径**交付 → 工具成功，界面上渲染出 `.artifact-card`，
   `.artifact-name` 里是文件名；
2. 用**相对路径**交付 → **必须被拒**（契约写明 files 只能是绝对路径或 http(s) URL，
   静默接受会让「产物」指向不可预期的地方），且拒绝原因含可操作信息；
3. 工具调用之后模型的**最终回复也渲染出来**。

第 2 条的断言落在**第二轮请求带回的工具结果**上 —— 那正是模型看到的东西，
而不是我们的推测。实测拒绝原因：
`present_files 的 files 必须全部是绝对路径或 http(s) URL。无效项：交付探针报告.md`

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| 设置页分组 | 22 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 29 |
| 对话框与降级 | 9 |
| 可视化卡片渲染 | 6 |
| 产物交付 | 6 |
| 主链路（mock 模型） | 11 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **364** |

## [0.1.4-zerowork.20]

### 新增测试：可视化卡片**渲染**（6 项，用 mock 模型，确定性的）

补的是一处零覆盖：`show_widget` 的**校验规则**已有单元测试（6 条拒绝规则、
render_mode 推断等），但**卡片是否真的画到界面上**从来没验过 —— 而那才是这个
功能的全部意义。工具返回对了、渲染层没接上，用户看到的就是「回复里什么都没有」，
而单测与 IPC 断言全都照过。

做法：mock 模型服务按轮次返回 ——

1. 第 1 轮：一个 `tool_calls`，调用 `show_widget`，参数是一段**合法 SVG**
   （恰好一个 `<svg>`、viewBox 为 `0 0 680 200` —— 这两条是工具的硬校验）；
2. 第 2 轮（请求体里出现 `role:"tool"` 说明工具已执行完）：普通最终文本。

然后用**真实 UI 路径**发消息（输入框打字 + Enter），断言：

- mock 收到**两轮**请求，且第二轮里有 `role:"tool"`（工具结果确实回传了）；
- DOM 里出现 `.widget-card`、`.widget-title` 等于预期标题、含 `.widget-frame`、
  **不是错误态**；
- 工具调用之后模型的**最终回复也渲染出来**。

**为什么用 mock 而不是真实模型**：这条验的是**渲染链路**，不是模型能力。
mock 让轮次完全确定，也就没有「弱模型不肯调工具」那类抖动。

实测：卡片标题「渲染探针图」，含 frame ✓。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| 设置页分组 | 22 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 29 |
| 对话框与降级 | 9 |
| 可视化卡片渲染 | 6 |
| 主链路（mock 模型） | 11 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **358** |

## [0.1.4-zerowork.19]

### 主链路测试改为走**真实 UI 路径**，并补上「回复渲染到界面上」的断言

原先主链路测试是直接调 `kami.prompt()`（IPC）发消息，断言的是「回复落到**会话状态**」。
但那证明的是「模型回话了」，**证明不了「用户在界面上看到了」** —— 渲染层完全可能
没把它画出来（消息列表坏了、流式回包没接上、组件抛错被吞），而所有断言照过。

补上 DOM 断言时**立刻假失败**，一查才发现差别有多大：

- 直接调 IPC 时，消息确实发出去了、回复也确实落进了会话状态，
  但界面**停在欢迎页** —— 因为「切到会话视图」是**界面自己的动作**，绕过它就不会发生；
- 换句话说，那条 DOM 断言在旧的发送方式下**永远只能是假失败**。

于是把发送步骤改成**走真实 UI 路径**：在 `[aria-label="消息输入框"]` 里
`fill()`（受控组件必须触发 React 的 onChange）再按 Enter。

这样这条用例验的就是用户真正做的那件事：**打字 → 发送 → 模型回话 → 看到回复**。
改完之后 11/11 全绿，DOM 断言也真的有了意义。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| 设置页分组 | 22 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 29 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 11 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **352** |

## [0.1.4-zerowork.18]

### 新增测试：设置页九个分组的 GUI 覆盖（22 项）

补的是一块**完全没被 GUI 覆盖过**的地方。`gui-sections.mjs` 覆盖的是**顶层导航**
那几项（助理 / 项目 / 专家·技能·连接器 / 自动化 / 资料库 / 诊断 / 统计），
而**设置对话框里的九个分组**——通用 / 个性化 / 记忆与进化 / 模型 / 内置运行时 /
数据管理 / 审计中心 / 提示词预览 / 关于——此前没有任何测试导航过，
全仓 grep 连这些标签都没出现过。

设置页是用户配置一切的地方；它渲染不出来、或某个分组点开是空白，属于
「界面看着在、功能全挂」那一类，只断言文本存在是抓不到的。

断言：

1. 齿轮按钮能打开设置对话框（`role=dialog` 的 `.settings-card` 出现）；
2. 侧栏分组清单包含**已知的九个标签** —— 少任何一个都会露出来
   （不是"数一下有几个"，而是逐名核对，删分组才抓得住）；
3. 逐个点开：面板渲染出**非空内容**、整卡文本长度合理、且**高亮的分组确实是刚点的那个**；
4. 每个分组都无渲染异常；
5. 关闭按钮能关掉对话框（关不掉也是这类 UI 的常见毛病）。

实测九个分组的面板文本长度：通用 / 个性化 / 记忆与进化 / 模型 773 字符 /
内置运行时 283 / 数据管理 83 / 审计中心 91 / 提示词预览 531 / 关于 77 —— 都有内容。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| 设置页分组 | 22 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 29 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **351** |

## [0.1.4-zerowork.17]

### 补充：记忆开关与内置「记忆整理」任务的联动（本地状态层 28 → 29 项）

「记忆」功能的**存储**（读 / 写 / 还原）此前已覆盖，但**开关与内置任务的联动**
没有 —— 而那是这个功能实际运转的方式：开启记忆会创建一个内置定时任务
（`builtin: true`、每晚无人值守整理），关闭时把它转成 `paused`。

断言（纯本地状态，不需要模型）：

- 开启记忆 → 列表里出现内置任务，`status: "active"`，且**提示词非空**（不是空壳）；
- 关闭记忆 → 同一个任务**仍在**，`status: "paused"` ——
  关掉开关不该把它删掉（那会把用户的改动一起丢掉）；
- 再开启 → 状态回到 `active`。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 29 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **329** |

## [0.1.4-zerowork.16]

### 新增测试：会话分支（6 项）

这也是一条**零覆盖**的用户功能：此前只测过「传不存在的路径会被明确拒绝」
（`bridge-rest.mjs`），也就是只验了失败分支。真正会用到的那条路 ——
有会话、有历史、从中间某个点分叉出去 —— 一行都没跑过。

断言：分叉返回 `{ok:true, branchPath, branchTitle}` 且 `branchPath` 是**另一个文件**；
新会话**真的落盘**（列表从 1 条变 2 条、无重复路径）；分叉后**当前会话切到分支上**
（这是该功能的语义）；**母会话没被改坏**（仍能复开并读出条目）。

#### 写这条测试时踩到并记下的两个竞态

都是**测试自己的问题**（产品行为是对的），已写进注释：

| 现象 | 根因 |
| --- | --- |
| 分叉报 `no-file` | `listSessions()` 会返回「桶里已有、文件还没写到磁盘」的 **pending 条目**（daemon 组装列表时的分支），拿那种路径去分叉，`existsSync` 为假。要先确认文件真的落盘 |
| 分叉报 `busy`（「正在生成，稍后再试」） | 提示词 resolve 只说明**消息发出去了**，daemon 侧的桶可能还标着运行中。要先等 `running === false` |

另外把「当前会话已切到分支上」这条断言改成**依赖分叉结果** —— 原先它独立读快照，
分叉失败时反而会**假通过**（读的是母会话的快照，条目当然读得出来）。

实测：母会话 `…80b1…jsonl` → 分支 `…a434…jsonl`，标题「…· 分支」，母会话复开后条目数 7。

### 顺带清理

删除 `scripts/recon/`（早前排查时的一次性探针目录，已空）。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| 会话分支 | 6 |
| **合计** | **328** |

## [0.1.4-zerowork.15]

### 新增测试：定时任务**执行**链路（4 项）

此前只测过定时任务的 CRUD（建 / 改 / 启停 / 删，见 `local-state.mjs`），
**执行**这一半一行都没跑过 —— 而「到点真的把任务跑起来」才是这个功能存在的意义。
CRUD 全绿而执行不通，用户看到的是「任务都在列表里，但从来没跑过」。

断言整条链路：

1. `automationRunNow` 入队后，调度器**真的起了会话、把提示词跑完**
   （调度器是入队即返回，所以按 `runs` 数组轮询等它落记录）；
2. 运行记录落盘：`runs[0]` 有 `success: true`、非空 `sessionId`、
   且 `finishedAt >= startedAt`；
3. 执行后任务仍在列表里（执行不该把任务弄丢）；
4. 探针任务清理干净（不把状态留给后续用例）。

实测：`success=true`、`sessionId=01a0c8aa-…`、耗时约 8.7 s。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| 定时任务执行 | 4 |
| **合计** | **322** |

## [0.1.4-zerowork.14]

### 新增测试：命令执行链路（5 项）—— 并据此更正一处**平台陈述不准确**

命令执行（`powershell` 工具 → 权限门 → 沙箱 / 降级 → 输出回传）是 Agent 的主干能力，
但此前**从未被端到端验证过**：`tool-loop` 用的是 pi 内置的 `ls`（只读文件工具），
应用自己这条链路一行都没跑过。

写这条测试时发现 README 的平台表**写错了**：那里写着「命令执行 ✅ 可用」，
实际在本机（macOS）**默认档下会被拒绝执行**。

#### 实情：命令沙箱是 Windows 专有

沙箱靠 `koffi` 调 Windows 的 `kernel32` / `advapi32`，所以 macOS / Linux 上
`sandbox.probe()` 必然 `available: false`，于是：

| 权限档 | 本机行为 |
| --- | --- |
| `read-only` / `workspace-write`（**含默认档**） | ⚠️ **命令被直接拒绝**，理由「为避免在没有操作系统写入约束的情况下执行命令」，并给出逃生指引 |
| `danger-full-access` | ✅ 可以执行（不经沙箱、直接 spawn） |

这是**有意的安全设计**：宁可不执行，也不静默地无约束执行 —— 否则「受限档」就成了摆设。
README 的平台表与新增的「关于命令沙箱」一节已据实更正。

#### 测试断言两侧都覆盖

- **安全性质**：默认档下命令必须被拦下，且审计里留下 `sandbox/blocked` 痕迹
  （实测证据：`沙箱不可用，命令未执行（当前系统不是 Windows：当前平台是 darwin）`）
- **功能性质**：切到完全访问档、**开新会话**后重试，命令真的执行，标记经工具结果回到模型回复
- **不得静默执行**：受限档下要么走审批、要么被拦下 —— 可移植的安全性质

#### 写这条测试时纠正的 3 处我自己的误解

| 我以为 | 实际 |
| --- | --- |
| 命令执行是 `ask` 档，「必须有权限弹窗」 | `danger-full-access` 下判定直接 `{kind:"allow"}`，**按设计不问**；受限档则在审批**之前**就拒了 —— 所以本机永远不会弹。改成断言「不得静默执行」这个可移植性质 |
| 完全访问档下审计里不该有 `blocked` | A 用例留下的那条记录本来就还在 —— 该看**增量**而不是绝对条数 |
| 同一会话里切档后重试即可 | 模型在同一个会话里已经收到「命令被拒」的结果，会一直纠结「要不要提权重试」而不动手。必须 `newTask` 开新会话 |

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| 命令执行链路 | 5 |
| **合计** | **318** |

## [0.1.4-zerowork.13]

### 新增单元测试：定时任务的时间计算与校验（21 项）

这块是**日期数学**，最容易写出「平时都对、跨天/跨周就错」的 bug，而且错了不报错、
只让任务在该跑的时候不跑 —— 端到端测试恰好覆盖不到。纯函数 + 固定时间点 = 完全确定。

钉住的判据：

- `nextRunAfter` **once**：时刻在未来返回它、已过返回 undefined（不再调度）
- **interval**：返回下一个整间隔边界，且**严格大于**当前时刻、上一个边界不晚于当前
  （否则就是跳过了一整个周期）；间隔非法时**抛错**而不是静默返回一个错时间
- **daily**：今天未到点→今天；已过点→明天；**正好等于该时刻→顺延到明天**；跨月正确
- **weekly**：本周未到点→本周；已过点→下周同日；多选时取最近的一天
- `validateSchedule`：间隔为 0/负/非整数、时间格式错、时间越界、weekly 没选星期 —— 都给出中文原因
- `scheduleSummary`：四种类型都能给出可读摘要；星期按**周一优先**排列（中文习惯，非周日打头）

**注意**：`nextRunAfter` 走本地时间，测试断言也用本地时间构造，不依赖时区。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 111 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| **合计** | **313** |

## [0.1.4-zerowork.12]

### 新增单元测试：权限判定规则（37 项）

`permission-rules.js` 是**安全关键**的一段：它决定模型的哪条命令要拦、哪个路径不许碰。
端到端测试只能验「权限请求发出去了」，验不了「该拦的到底拦没拦」—— 而后者才是
安全性所在的判据。实现是纯函数，用单测逐条钉住：

- `defaultProtectedDirs`：凭据落点清单（`.ssh` / `.gnupg` / `.aws` / `.kube` / `.docker`
  / `.npmrc` / `.git-credentials`）齐全且都是绝对路径
- `isPathContained`：自身与子路径算包含；外部不算；**目录穿越逃不出去**；
  **同前缀不同目录不算包含**（`/ws-other` 以 `/ws` 开头，朴素前缀匹配会在这里放行 —— 这条专治那类漏洞）
- `classifyCommandOperation`：读 / 写 / 删 / 最轻档各举若干真命令
- `isOpaqueCommand` / `isScriptExecution`：编码命令、`python -c`、`node -e`、
  `cmd /c`、直接跑 `.ps1`/`.sh`
- `isConfigAsCodePath`：`package.json` / `.npmrc` / `.envrc` / `.pi/settings.json` 受管；
  而 `.pi/skills/**` 是**刻意留的例外**（能力即数据、加载时不执行）
- `extractPathCandidates`：从命令里挑出路径候选

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 90 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| **合计** | **292** |

## [0.1.4-zerowork.11]

### 新增单元测试：应用自有 agent 工具的校验规则（34 项）

这些工具（`todo_write` / `use_skill` / `show_widget` / `read_me`）此前只有在**模型主动
调用**时才被执行到 —— 端到端测试只能覆盖「循环转得起来」，覆盖不到每个工具**自己的
校验逻辑**。而那正是最容易写错、也最该兜住的部分：`show_widget` 有 6 条拒绝规则、
`todo_write` 有全量替换语义、`use_skill` 有 3 种不可加载的情形。

它们的实现是**纯函数**，用单测跑既快又确定（53 项单元测试总耗时不到 1 秒），
不必依赖模型、不依赖网络。测法：既直接测导出的纯函数
（`validateWidgetCode` / `normalizeTitle` / `normalizeLoadingMessages` / `parseModules` /
`confirmText`），也用一个**假的 `pi`** 把工厂注册出来的工具抓下来、直接调它的 `execute`。

**写这批测试时纠正了 4 处我自己的误解**（实现都是对的，错的是我的预期）：

| 我以为 | 实际 |
| --- | --- |
| `todo_write` 回执会展示 `activeForm`（「正在补文档」） | 回执用的是 `content`（「补文档」）；`activeForm` 是**执行期间界面上**轮播的短语，由渲染层消费 |
| `normalizeLoadingMessages('{"not":"array"}')` 应返回 undefined | 只有以 `[` 开头的串才走 JSON 解析；其余按逗号切分，于是整串成为一条文案 |
| `normalizeLoadingMessages("[broken json")` 会退回逗号切分 | `[` 开头但 JSON 损坏 → **明确返回 undefined**，不静默降级 |

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 53 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| **合计** | **255** |

## [0.1.4-zerowork.10]

### 新增测试：桥接面补测（28 项，不需要模型）

`preload` 暴露 117 个方法，此前测试覆盖 77 个 —— 剩下 40 个**一行都没跑过**。
其中约 30 个不需要模型、不需要网络，纯粹是「调一下看它答什么」，没有理由不测。

覆盖：会话轴（场景 / 交互模式 / 推理强度 / 专家，含非法值拒绝）、
提供商 CRUD 往返（保存 → 读回 → 加模型 → 删除 → 读回报错）、运行时管理
（总开关切换与还原、未知 id 的处理）、MCP 切换、会话生命周期、
团队与自动化的未知 id 拒绝、产物路径校验、界面应答、设置测试类、
以及**全部 9 个事件订阅通道**（可订阅、返回注销函数、注销不抛）。

**断言口径**：这些通道此前从没被调用过，**不能假设它们一定成功**。所以断言的是
「**行为明确**」而不是「一定成功」：读取类必须有形状、变更类要么成功要么以指明
原因的错误被拒、**一律不得挂起**（挂起是这类故障最糟的形态 —— 界面永远等一个不来的结果）。

写这个测试时纠正了两处**我自己的误解**（契约本身是合理的）：

| 通道 | 我起初以为 | 实际 |
| --- | --- | --- |
| `restartSessionFrom` | 传不存在的路径应**抛错** | 返回**结构化失败** `{ ok: false, reason, message }`，不抛 —— 与 `resume` / `delete` 的「抛错」语义确实不同：一个是「参数不对」，一个是「分支操作失败但可继续」 |
| 运行时总开关 | 读 `settingsSnapshot().runtimeMaster` | 读 `runtimesSnapshot().master`（`settingsSnapshot` 是模型目录快照，不含它） |

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 桥接面补测 | 28 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| **合计** | **221** |

## [0.1.4-zerowork.9]

### 清理悬空引用：指向已不存在文件的注释

注释与文档里有一批引用指着不存在的路径。这类引用不是品牌问题，却是**实打实的
误导** —— 读者按图索骥会扑空，严重时以为是自己没找到。本轮按「引用的东西还存不存在」
逐类清理：

| 类别 | 例 | 处理 |
| --- | --- | --- |
| 已合并进本文件的样式内容 | `tokens.css`、`index.css` | `app.css` 改为指向本文件的 `:root`；两段自相矛盾的注释（说「本文件不再自带 :root 定义」，而 :root 就在本文件里）整段重写 |
| 已并入 daemon 的模块 | `src/core/*.ts`、`prompt-composer.ts`、`node.ts`、`artifact.ts`、`bundled-payload.ts` | 改为本仓库真实路径（`src/main/daemon/*.js` 对应模块） |
| 构建工具链（不在本仓库中） | `electron-builder.yml`、`scripts/fetch-{node,gitbash,uv}.mjs`、`check:tokens` 校验、`src/renderer/fonts/` | 改为说明「不在本仓库中」 |
| 未收录的测试文件 | 引用不存在的单测 | 改为说明「不在本仓库中」 |

`DESIGN.md` 有个容易搞混的分叉：`resources/experts/mvp-dev-expert-team/` 里的指的是
**用户自己项目生成的设计契约**（`项目/DESIGN.md`），那条**不能改**；只有
`src/renderer/src/` 里指的是本产品的设计文档。已按此区分处理。

**新增测试能力**：`restore-renderer.mjs` 改为**可导入**（导出两个注释清理函数、
仅在直接运行时才执行还原），这样这两个**文本变换**函数能被单测覆盖 ——
它们写错会静默改坏生成物里的注释。已实测：对同一输入三次应用结果一致（幂等），
且「对原始产物跑完整管线」与「在当前结果上跑新步骤」逐字节一致（管线可复现）。

### 删除调试遗留

`scripts/recon/`（`dbg-scope.mjs`、`list-symbols.mjs`、`symbols.json`，共 110KB）
是早期排查时的一次性诊断脚本与生成输出，无任何引用。已删除。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| **合计** | **193** |

## [0.1.4-zerowork.8]

### 修复：模块图校验那道 CI

原来的检查有两处问题：

1. **跑不起来。** 它重跑 `decompile:daemon` 并逐字节 diff，而该命令的输入
   `.pristine/daemon.original.js` 在 `.gitignore` 里、不在仓库中 ——
   干净检出下必然失败。也就是说**这道 CI 一直是红的**。
2. **逐字节比对的前提不成立。** 源码之上还有真实修复与注释改写，
   文本天然与工具产出不同。

改成 `npm run check:daemon-graph`，守**真正该守住的性质**：模块图闭合 ——
每个相对 import 指向真实文件、每个具名 import 都能在目标模块的导出里找到。
漏导出、写错路径一定在这里露出来，注释怎么改都不会。

## [0.1.4-zerowork.7]

### 命名与元数据统一

- 插件市场目录：`cb_teams_marketplace/` → `teams_marketplace/`，
  各插件目录下的插件清单目录统一为 `.zerowork-plugin/`
- 股票技能命名统一：`westock` → `stock`、`westock-data` → `stock-data`、
  `westock-tool` → `stock-tool`、连接器名 `westock-mcp` → `stock-mcp`
- 各插件清单的 `author` 统一为 ZeroWork、`license` 统一为 Apache-2.0

`resources/` 下成片的「来源说明」README 改写为**功能文档**（这个专家/资源是什么、
包含什么、怎么加载、有哪些已知限制），保留原有技术内容。

### 新增：外部资源请求审计清单

`EXTERNAL_REQUESTS.md` —— 逐条列出**运行时会向外部发起的请求**：地址、用途、
触发条件、能否关闭。分三部分：应用本体（全部可控）、随包资源（部分不可控）、
已移除的请求。

其中「已移除」一项值得单列：股票专家包自带的 `bin/init_task.py` 原本是一个
**遥测上报器** —— 生成设备 UUID 落盘、向远端 InLong 端点 POST
`task_start` / `task_complete`（含耗时、成败、设备标识、由工作区 `.git` 路径
推出的会话键）。本仓库**已移除全部网络上报**，改为纯本地 no-op（保留同名命令，
让正文里的调用不至于失败）。

### docx 测试：增加不依赖模型的确定性引擎往返

原 docx 往返用例由真实模型触发工具。本机端点是能力较弱的模型，它经常把整个
输出预算耗在「该不该用 docx 技能 / 该不该先 present_files」这类推理上，
**工具一次都不调**，用例便报「模型未回报完成」—— 看起来像引擎坏了。

已用**基线复现排除回归**（改写前的状态同样失败），并做两件事：

1. **新增确定性验证**：按应用自己的调用契约直接跑引擎
   （`python -m html_to_docx convert …` + `PYTHONPATH=<engineDir>`，与
   `doc-extract.js` 完全一致），反向 `-m docx_to_html extract`，
   断言标记双向存活。这条**不依赖模型**，是「引擎能不能用」的权威证据。
2. **修掉用例自身的配置过紧**：provider 声明的输出上限原为 4096，
   弱模型常在动手前耗尽；提示词结尾原为「只回复 CONVERT_DONE」，
   与系统提示词的交付纪律冲突，模型会卡在矛盾里。现已改为顺着交付纪律提问，
   断言落在**产物文件本身**而非「模型说了某个魔法字符串」。

实测：`docx 引擎：HTML → DOCX → HTML 往返` 通过（36687 字节 docx → 259 字符
html，标记存活），模型驱动的两条工具用例也通过。该层 8 → 9 项。

### 测试规模

## [0.1.4-zerowork.6]

### 修复：平台不适用的托管运行时不再出现在清单里

**问题**：`gitbash` 运行时是 Git for Windows 的 PortableGit 包
（`.7z.exe`，Windows 专有）。在 macOS 上不过滤的话，UI 会显示
「bash 运行时：未安装」并给出安装按钮 —— 用户点下去会下载一个跑不起来的
Windows 自解压程序，然后困惑地失败。而 macOS/Linux **本来就自带 bash**，
这个运行时在那两个平台上根本不适用。

**修复**：新增 `RUNTIME_PLATFORMS` 表，在 `runtimeDescriptors` 一处过滤。
运行时清单（UI）与 shell 注入都从该函数派生，所以一处过滤同时修正
「界面误导」与「注入不该注入的东西」。

```js
const RUNTIME_PLATFORMS = {
  node:    ["win32", "darwin", "linux"],
  python:  ["win32", "darwin", "linux"],
  gitbash: ["win32"]   // PortableGit 是 Windows 专有；其它平台系统自带 bash
};
```

**实测（macOS）**：清单为 `python=ready`、`node=missing`，gitbash 不出现。

### 新增测试：技能调用与 MCP 连接器

这两块此前**一行都没测过**，而它们都不属于「环境做不到」：

- **技能调用**：`/skill:<name>` 的展开发生在 daemon 的 **prompt 入口**。
  测试要求模型回报技能正文里的**第一个标题** —— 只有「解析技能 → 读文件 →
  剥 frontmatter → 注入提示词 → 模型收到」整条链路都在，它才答得出来。
- **MCP 连接器**：`tests/e2e/mock-mcp-server.mjs` 是手写的最小 stdio MCP server
  （`initialize` / `tools/list` / `tools/call` 三个方法，换行分隔 JSON）。
  **故意不引官方 SDK** —— 引了会把 SDK 自己的行为也带进来，出问题时分不清
  是谁的锅。断言覆盖：配置校验（含 `command` 与 `url` 并存须被拒）、
  连接建立、工具被识别、模型调用并拿到结果、以及**权限请求确实发出**。

最后一条是**安全断言**而非功能断言：MCP 工具的权限判定是 `ask`
（等同「执行用户配置的任意命令」）。若权限请求数为 0，说明审批链路被绕过了。

```bash
npm run test:gui:skill   # 10 项，需要本机有可用模型端点
```

### 排查记录：一个把两个用例卡了很久的时序坑

两个 MCP 用例曾长期失败，症状是「新会话的 MCP 客户端一直停在 `connecting`」。
根因不在连接本身，而在**读状态的时机**：

- MCP 客户端是**按会话桶**建的（`mcpHandleByBucket`），连接发生在
  **会话构造期** —— 扩展工厂在 `resourceLoader.reload()` 里被 `await` 跑完，
  连完才求值 `extraActiveTools()`。所以「连接完成」与「工具名进白名单」的
  顺序在构造内部就已保证，外部无需等待。
- 但桶里**还没构造过会话**时，`mcpConfigGet` 取不到活句柄，只能按配置
  **合成**一个 `connecting` 占位（`session-files.js` 的回退分支）。
  那个 `"connecting"` 不是「正在连」，而是「还没开始连」——
  轮询它等 `connected` 会一直等下去。

错的是测试：`newTask(ws)` 之后只轮询不发消息，会话永远不构造。
改成**直接发消息**（发消息才构造会话）后，连接与白名单在构造内部自然完成。
这条坑已写进测试注释与 README，避免后来者重踩。

### 新增测试：本地状态读写（28 项，不需要模型）

补的是 IPC 通道里此前**完全没被驱动过**的一大片。它们有个共同点：
不依赖模型、不依赖网络，纯粹是「界面写进去 → 落盘 → 读回来」——
恰好是用户天天在设置页里点的那些开关，没有任何理由不测。

覆盖：设置往返（风格、推理强度、记忆开关与正文、用户画像、个性化、权限、
Agent 团队、默认工作区路径、Web 搜索）、工作区创建与显示名校验、
会话列表与补全项、用量统计与运行台账、审计的列表/导出/清空、
定时任务 CRUD 与非法入参拒绝、技能开关往返、`statPath` 三种结果。

断言取向是**往返优先**：「调用了不报错」证明不了任何事 ——
通道名拼对但 handler 是个空函数，照样不报错。所以设置类一律
「读当前值 → 写新值 → 读回来断言变了 → 还原 → 断言变回去」。

#### 三条与直觉相反、但**都是正确的**产品语义

写测试时先按直觉写了断言，跑失败后逐条查实现，发现错的是断言不是产品。
三条都写进了断言与注释，避免后来者当成 bug 去"修"：

| 行为 | 直觉 | 实际 |
| --- | --- | --- |
| `auditClear()` | 清空后 0 条 | 剩 **1 条** `audit/cleared`。`clearAuditRecords` 删完文件会立刻补写这条 —— 「擦除审计日志」这件事本身必须留痕，否则谁都能悄悄清干净。这是**有意的安全设计**。 |
| `removeWorkspace()` | 删掉磁盘目录 | **不删**目录。目录里是用户真实文件，凭「移出空间列表」就删会丢数据。它只做两件事：把该空间的会话移进回收站、清掉显示名。 |
| `statPath()` 传不存在的路径 | 抛错 | 返回 `{ kind: "missing" }`。不存在是**正常返回值** —— 调用方要靠它决定「先读还是先建」。 |

另有两处接口形状与通道常量名不同，也一并记下：暴露出来的是
`listAutomations` / `saveAutomation` / `toggleAutomation` / `deleteAutomation`
（`automationXxx` 是通道常量名）；`getWebSearchConfig` 只回传
`providerId` 与 `hasKey`，**不回传 API Key 本身**（凭据不该往渲染层送）。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 本地状态读写 | 28 |
| 对话框与降级 | 9 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| docx 往返 + 运行时 | 9 |
| 技能调用 + MCP 连接器 | 10 |
| **合计** | **193** |

---

## [0.1.4-zerowork.5]

### 修复：Node 运行时改为跨平台，macOS 上真正可用了

**问题**：`NODE_ARTIFACT` 硬编码 `win-x64.zip`、`NODE_REQUIRED_FILES` 要求
`node.exe`，导致在 macOS/Linux 上安装必然失败（下载 Windows 包 → 在探针相位
跑不起 `node.exe`）。这不是"平台限制"，是**代码没做平台适配**。

**修复**：新增 `NODE_PLATFORM_SPECS` 表，按 `process.platform-arch` 取规格：

| 平台 | 产物 | 可执行文件 | 归档校验 |
| --- | --- | --- | --- |
| `win32-x64` | `node-v22.23.2-win-x64.zip` | `node.exe` | 与官方清单交叉核对 |
| `darwin-arm64` | `node-v22.23.2-darwin-arm64.tar.gz` | `bin/node` | 同上 |
| `darwin-x64` | `node-v22.23.2-darwin-x64.tar.gz` | `bin/node` | 同上 |

同时新增**最小 tar 读取器 + gzip 解压**（纯 JS，不引第三方依赖）——
原实现只支持 zip。未列出的平台**明确报错**，不回落到 Windows 规格。

**修的两个坑**（都是这次才暴露的）：
1. tar 的 mode 字段（偏移 100）是**八进制 ASCII 字符串**，不是二进制整数。
   按二进制读会得到错的权限位，解出来的 node 没有可执行位，
   探针以 `EACCES` 失败 —— 实测踩到。
2. 归档条目要防目录穿越（`../` 逃出目标目录）。

**实测结果**：

    安装前：missing
    执行安装：ok
    安装后：ready

`status: ready` 意味着**探针真的执行了 `bin/node --version` 并解析出版本号**，
即解出来的二进制确实可执行。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| 对话框与降级 | 9 |
| docx 往返 + 运行时 | 7 |
| **合计** | **153** |

---

## [0.1.4-zerowork.4]

### 新增：docx 引擎往返测试 + 运行时安装失败质量测试

**docx 引擎（Python 路径）** —— `docx_convert` / `docx_extract` 是两个 agent
工具，走 `resources/docx-engine` 的 Python 实现。Python 运行时跨平台且本机
为 ready，因此可以真实跑通。

用**往返**验证：HTML（埋标记）→ DOCX → HTML（取回标记）。
标记能穿过两个方向，说明转换与提取两条路都正确 —— 单独测某一个方向都可能
因「转换时丢内容、提取时碰巧没丢」而假阳性。实测通过。

### 更正：运行时规格平台硬编码，但**不阻塞功能**

（上一节结论已更正，保留原文以便追溯。）

**关键更正**：托管运行时是**可选加速器**，不是运行前提。
`planRuntimeInjection` 对未就绪的运行时给出 `not-ready` 决策并**跳过注入**，
脚本照常用系统 PATH 里的工具执行。已在 `tests/e2e/docx-runtime.mjs` 中加入
两条断言固定这一点：
  - 托管运行时缺失时，注入决策为 not-ready 而非报错
  - 托管运行时缺失时，命令执行仍可用

原始发现如下（仍然成立）：

测试 node 运行时安装时失败，错误信息精确指出：

    安装「Node」失败（相位 probe-executable）：探针没跑通
    （进程没起来：spawn .../node.exe ...）

核查源码确认：

```js
const NODE_ARTIFACT    = `node-v${V}-win-x64.zip`;        // win-x64 硬编码
const NODE_REQUIRED_FILES = ["node.exe", "LICENSE"];      // node.exe 硬编码
const GITBASH_ARTIFACT = `PortableGit-...-64-bit.7z.exe`; // Windows 专有
```

即在 macOS/Linux 上，托管 Node 与 Bash 运行时**注定装不上**。

**但失败得优雅** —— 有相位、有原因、不挂起、失败后状态回到可重试态。
这才是该断言的：平台不支持可以接受，静默挂起或留脏状态不可接受。
测试据此改为断言「失败的质量」，已通过。

已在 README 增加「平台支持说明」表，明确哪些能力跨平台、哪些是 Windows 专有。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| 对话框与降级 | 9 |
| docx 往返 + 运行时 | 6 |
| **合计** | **152** |

---

## [0.1.4-zerowork.3]

### 新增：对话框与原生模块降级测试

把两类此前标为「测不了」的能力，尽量测掉属于本工程代码的那部分。

**① 系统文件对话框** —— 原生对话框 UI 由操作系统绘制，确实无法自动化。
但**应用对对话框结果的处理**可以测：Playwright 的 `app.evaluate()` 能在
**主进程**里执行代码，因此可以 stub 掉 `dialog.showOpenDialog`，构造各种返回值。

覆盖：
  - 选择工作区目录：选中时返回路径
  - **选择工作区目录：用户取消时静默返回 undefined**（不抛错、不挂起）
  - 选择技能目录：取消时静默返回
  - 选择输入文件：取消时静默返回
  - **选择输入文件：选中真实图片文件时的编码处理**

取消路径尤其值得测 —— 挂起是这类交互最糟的故障形态：
界面一直等着一个永远不来的结果，用户不知道发生了什么。

**② Windows 专有原生模块** —— `koffi`（沙箱用 DLL）与 `@napi-rs/canvas`
（pdfjs 渲染用）只有 win32 二进制，本机根本加载不了。这里断言的不是
「这些功能可用」（物理上不可能），而是**缺失时应用是否优雅降级**：
  - koffi 缺失不影响沙箱探测
  - @napi-rs/canvas 缺失不影响 PDF 文本提取（canvas 只用于页面栅格化，
    文本提取走另一条路径）
  - 应用在缺少 win32 模块时正常运行

⚠️ 这一层**不能替代**在 Windows 上的验证。它证明的是降级行为正确，
不是 Windows 专有功能可用。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| 对话框与降级 | 9 |
| **合计** | **146** |

---

## [0.1.4-zerowork.2]

### 新增：文档解析工具链测试（补上最后一处未验证链路）

`read_document` 不是 IPC 通道，而是**暴露给模型的工具** —— 没有模型就没人
调用它，所以此前一直标为「未验证」。有了真实模型端点后即可覆盖。

`tests/e2e/doc-parsing.mjs`（4 项断言）：
  - 配置真实模型并切换工作区
  - **PDF 提取**：模型读出文档里埋的标记 ZWPDF42
  - **DOCX 提取**：模型读出文档里埋的标记 ZWDOCX42
  - 无渲染层未捕获异常

标记法：正文里埋唯一标记，模型按提示原样回报。只有「提取成功 + 回传成功」
两个条件同时满足，标记才会出现在回复里 —— 单一断言覆盖整条链路。

测试自带夹具（PDF 手写对象结构、DOCX 用 jszip 打包 OOXML），
不依赖外部预置文件。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型 | 4 |
| 文档解析 | 4 |
| **合计** | **137** |

---

## [0.1.4-zerowork.1]

### 变更：全面品牌改写为 zerowork

按开源发布要求，移除全部原有公司品牌字样。

本次发布将原有品牌标识全面替换为 zerowork，涉及四类标识：

- **npm 包名** —— 用于 `package.json` 与锁文件
- **产品显示名** —— 出现在窗口标题、欢迎页、应用菜单
- **环境变量前缀**（9 个）—— 用于配置目录、资源目录、工作区目录、运行时目录等
- **默认目录** —— 配置目录与工作区根目录

精确的标识映射关系见 git 提交历史（`git show <本次提交>` 的 diff）。
不在本文件重复列出旧标识，是为了让仓库中不残留任何旧品牌字符串。

**迁移提示**：如果你此前用过旧版本，配置目录与工作区目录的位置都变了，
需要把旧目录内容手动迁到新位置，或通过环境变量指回旧路径继续使用。

共改写 **47 个文件、176 处**。

**同时移除公司内网的 provider 端点**：原版在 `BUILTIN_PROVIDERS` 里硬编码了
一个企业内网 API 地址，那属于企业资产、且对下游使用者是不可达的死配置。
现改为空数组，使用者通过设置页自行添加服务商。

⚠️ 改写时甄别出一处**误报**：`src/renderer/src/office-xlsx.js` 中有一串
base64 图片数据，恰好包含被替换的三个字母。已按设计跳过 —— 盲目全局替换
会毁掉图片。这类误报是批量改写必须逐个甄别的原因。

### 新增：真实模型端到端测试

mock 只能证明管道不漏，证明不了协议对接正确。新增 `tests/e2e/real-model.mjs`
用真实 LLM 验证端到端对话：

- 埋唯一标记要求模型原样回复，校验回复内容
- **校验回复携带真实 token 用量** —— 这是"确实调用了真实模型"的硬证据
- 凭据运行时从 `~/.claude/settings.json` 读取，**不进仓库、不打印**
- 端点不可用时整轮跳过（exit 0），不让 CI 因缺本机环境而失败

实测结果：模型回复了 `ZW608933`，
用量 `{"input":773,"output":4,"cacheRead":17152,"totalTokens":17929}`。

### 许可

补齐 `LICENSE` 文件（Apache License 2.0），并重写 README 的许可章节。

**为什么是 Apache-2.0**：本项目源自企业产品，该许可含明确的专利授权条款
（第 3 节），下游企业采用时法务顾虑最小；相比 MIT 多一层专利保护，
相比 AGPL 对下游更友好。

**为什么要在 README 里显著标注**：选择许可证是权利人的法律决定，不应由
还原者代为拍板。README 列出了发布前必须确认的三件事 —— 许可选择本身、
`LICENSE` 末尾的版权署名主体、第三方依赖的条款核对。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试 | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| 真实模型（需本机端点） | 4 |
| **合计** | **133** |

---

## [0.1.4-restore.4]

### 新增：主链路端到端测试（此前唯一的空白）

**「发一条消息，模型真的回话了没有」—— 这是本项目最重要的一条测试，
此前所有测试都只在验证应用自身的零件。**

常规做法需要真实 API Key，但应用支持自定义 provider（`baseUrl` 可配），
于是在本地起一个 OpenAI 兼容的 mock 服务，把整条链路真正串起来：

```
window.kami.prompt() → preload IPC → daemon → pi agent harness
  → HTTP POST /v1/chat/completions → mock 服务 SSE 流式回包 → 会话状态
```

10 项断言覆盖：注册 provider、写入 API Key、切换模型、
**mock 服务收到请求**、**请求体含用户消息**、**携带 Authorization 头**、
**模型回复落到会话状态**。

断言的是链路真的通了，不是模型能力。mock 服务用 `node:http` 手写，
不引第三方依赖 —— 测试基建引入的依赖越多，它自己出问题的概率越高。

`npm run test:gui:model`

### 新增：Agent 工具调用循环测试

在 mock 服务上再进一步：让它分两轮响应 —— 第一轮返回 `tool_calls` 要求执行 `ls`，
第二轮在收到工具结果后返回最终文本。

6 项断言，关键是后两条：
  - **mock 服务收到至少两轮请求** —— 只收到 1 轮说明工具调用没被处理
  - **第二轮请求里带 `role:"tool"` 的消息** —— 证明工具真的执行了且结果回传了

这验证的是「agent 能不能干活」，而不只是「模型能不能回话」。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试（Vitest） | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| 主链路（mock 模型） | 10 |
| Agent 工具调用循环 | 6 |
| **合计** | **129** |

---

## [0.1.4-restore.3]

### 新增

- **IPC 功能性测试**（`tests/e2e/ipc-functional.mjs`，63 项断言）：
  逐个驱动 preload 暴露的桥接方法，验证**真实应答**而非仅渲染。
  覆盖三层：
  1. 28 个无参只读通道 —— 断言调用成功且返回有效数据
  2. 读写往返 —— 布尔/字符串型设置项「读取 → 修改 → 再读 → 复原」，
     验证状态真的持久化；另有技能开关、权限设置、MCP 配置、
     自动化任务、工作区创建的完整往返
  3. 带参通道 —— 从已有快照取真实参数后调用

  这一层补上了关键缺口：前两层测试只证明「界面能渲染」，
  证明不了「某个功能能不能用」。通道名拼错、handler 未注册、返回结构
  变了，界面可能照常渲染（数据区空白或停在加载态），只有真正调用才会暴露。

### 修复

- **测试污染用户家目录**：配置目录用 `ZEROWORK_CONFIG_DIR` 隔离了，
  但工作区根目录默认落在 `~/ZeroWork`，创建类测试会在用户真实家目录下
  建目录。已改为显式设置 `ZEROWORK_WORKSPACE_DIR`，三个 e2e 测试全部隔离。
  （发现时已清理掉产生的残留目录）

### 测试编写中修正的自身缺陷

IPC 功能性测试第一轮 36/41，5 项失败经核对**全部是测试自身写错**，
不是应用缺陷。逐个确认后修正：

| 失败项 | 真实原因 |
| --- | --- |
| `hiddenContext` 返回 undefined | JSDoc 明写「还没跑过任何一轮为 undefined」，是正确的空态语义 |
| `promptPreview` 报错 | 参数形状是 `{ sceneId, modeId, expertId? }`，测试传错了 |
| `runtimeDiagnostics` 报错 | 需要运行时 id，测试无参调用 |
| `automationList` 方法不存在 | 桥接方法真名是 `listAutomations` |
| `previewBaseUrl` 返回 undefined | 需要 cwd；无预览服务时返回 undefined 属正常 |

第二轮又修正三处接口契约理解错误：

| 失败项 | 真实契约 |
| --- | --- |
| `saveAutomation` 报 trim 错误 | 要 `{ name, prompt, schedule, cwd }`，测试用了 `title` 且漏 `cwd` |
| `mcpConfigSet` 报 charCodeAt 错误 | 收 **JSON 字符串**，测试传了对象 |
| 工作区创建后列表找不到 | `listWorkspaceGroups` 从**会话的 cwd** 派生，新建的目录尚未被会话使用；且 `getDefaultWorkspacePath` 返回 `{ effective, isDefault }` 而非字符串 |

这些契约细节原本没有任何文档，现在都写进了测试注释。

### 测试规模

| 层次 | 断言数 |
| --- | --- |
| 单元测试（Vitest） | 19 |
| GUI 启动健康度 | 17 |
| GUI 分区域 | 14 |
| IPC 功能性 | 63 |
| **合计** | **113** |

---

## [0.1.4-restore.2]

### 变更

- **daemon 拆分**：`src/main/daemon/index.js` 从 19,467 行单文件拆成
  **40 个领域模块**（配置路径 / 自动化 / 归档 / 记忆 / 模型 / 审计 /
  事件日志 / 专家 / MCP / 权限 / 提示词组装 / 会话宿主 / 运行时 /
  文档抽取 / 命令执行 / 工具工厂 / 团队 / 工作区 / Git worktree …）

  拆分过程中修复三个正确性缺陷（均由构建或端到端测试暴露）：

  1. **可变绑定跨模块赋值** —— ESM 的 import 绑定只读，`let x` 被别的模块写
     会直接构建失败。改为把「声明单元 + 所有写入单元」用并查集绑定，强制同模块。
  2. **循环依赖触发 TDZ** —— 模块成环时，某个模块会在依赖未求值完时执行顶层
     代码，报 `Cannot access 'x' before initialization`。
     改为在 import 图上求强连通分量并合并。
  3. **判据必须两级** —— 只在初始化期依赖上找环会漏掉「A 初始化期依赖 B、
     B 运行时依赖 A」这种环；不加区分地在完整图上合并又会让 39 个模块
     退化成 1 个。最终判据：完整 import 图上找环，且仅当环内含初始化期边时合并。

  同时改进：import 按 specifier 拆分后按需分发；依赖模块的 import 顺序按
  各模块首个单元在原文中的位置排列，保证模块求值次序与原文一致。

- **测试覆盖扩展**
  - 新增分区域 GUI 测试（`tests/e2e/gui-sections.mjs`）：逐个导航到侧边栏
    七个区域，验证每个区域真实渲染且无异常，共 14 项断言
  - `npm run test:gui` 现在包含启动健康度 + 分区域两轮，共 31 项断言
  - CI 新增模块图校验任务（后收敛为 `npm run check:daemon-graph`）

### 修复

- 拆分工具副本污染：拆分结果曾覆盖被拆分的源文件，导致后续运行输入错误且
  静默产出无意义结果。此后原件单独保存只读副本，不再用拆分结果覆盖源文件

### 注意事项

分区域测试中有三个区域（助理 / 项目 / 资料库）在无数据时渲染的是欢迎页本身，
不是独立页面。测试里已按实测行为标注，判断导航是否生效的断言对它们不适用。
这一点是跑测试时发现的——最初一律用「欢迎页标语消失」判断，把这三个正常页面
误判为故障。

---

## [0.1.4]

首次发布。

### 基线

- 应用版本：`0.1.4`
- 运行时：Electron 44（Chromium 152.0.7977.54 / Node 24.18.1）

### 新增

- **构建期工具**：chunk 拆分与渲染层重建脚本。用真实作用域解析建立依赖图，
  避免把对象属性名 / 局部变量误判为跨模块引用；处理 `__vite__mapDeps`
  预加载表与静态资产路径
- **主进程与渲染层**
  - `src/main/`、`src/preload/`、`src/shared/`：模块边界整理，
    导出符号从 Rollup 的字母别名（`D as DEFAULT_GLOBAL_SHORTCUT`）规范化回语义名
  - `src/renderer/`：29 个 chunk 使用语义化文件名
  - `resources/`：场景、模式、专家、技能、样式等运行时资源
- **构建系统**：`electron.vite.config.mjs`
- **测试**
  - 单元测试：IPC 契约完整性、`docKindOf` 文档类型判定、模块依赖图正确性
  - 端到端 GUI 测试：Playwright 驱动 Electron，17 项断言覆盖界面渲染、
    daemon 存活、IPC 应答、样式加载
- **工程治理**：README、CONTRIBUTING、SECURITY、ESLint、Prettier、
  EditorConfig、tsconfig、GitHub Actions CI

### 修复（还原过程中发现）

- **剥离构建期注入的 CommonJS shim**：产物中已含 electron-vite 注入的
  `__cjs_mod__` / `__filename` / `__dirname` / `require`，重新构建时会重复注入
  并报 `"__cjs_mod__ has already been declared"`
- **Vite 运行时助手改名**：产物中的 `__vitePreload` / `__vite__mapDeps`
  与重新构建时 Vite 注入的同名助手冲突，改为工程自有符号
- **补回入口样式表链接**：首次还原 `index.html` 时漏掉 `<link rel="stylesheet">`，
  导致界面无样式渲染。此问题由端到端测试的样式断言捕获

### 已知限制

- **TypeScript 类型已丢失**，无法从产物恢复。当前以 JS + JSDoc 提供，
  `checkJs` 暂时关闭（见 `tsconfig.json` 注释）
- **模块边界为重建结果**，非原始边界。渲染层为 chunk 粒度而非组件粒度
- **`src/main/daemon/index.js` 尚未拆分**（约 1.9 万行）。
  拆分工具已就绪，分组清单待编排
- **`ZEROWORK_` 环境变量前缀**与产品当前名称不一致，属历史遗留命名技术债

### 未验证

以下功能因依赖 Windows 专有的原生模块或外部服务，**未在 macOS 环境验证**：

- `koffi`（FFI，仅提供 win32-x64 二进制）
- `@napi-rs/canvas`（仅提供 win32-x64-msvc 二进制）
- 需要登录凭据的模型供应商与云端能力
- 依赖 Windows 运行时（如 gitbash 运行时）的功能

上线前需在 Windows 环境补测。
